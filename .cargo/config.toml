[env]
# Always build the Cargo.lock-pinned, checksum-verified AWS-LC source. Without
# this guard aws-lc-sys may auto-discover a host library, making CI, release,
# and operator builds depend on mutable machine state.
AWS_LC_SYS_USE_SYSTEM = { value = "0", force = true }
