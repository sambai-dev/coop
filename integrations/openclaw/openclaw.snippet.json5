// Merge this block into ~/.openclaw/openclaw.json. Keep the real values in
// the Gateway process environment; do not commit credentials into config.
{
  mcp: {
    servers: {
      rookhold: {
        command: "rookhold-cli",
        args: ["mcp-server"],
        env: {
          ROOKHOLD_BASE_URL: "${ROOKHOLD_BASE_URL}",
          ROOKHOLD_API_KEY: "${ROOKHOLD_API_KEY}",
          ROOKHOLD_MCP_MINIMUM_ISOLATION: "${ROOKHOLD_MCP_MINIMUM_ISOLATION}",
          ROOKHOLD_MCP_ALLOWED_LANGUAGES: "${ROOKHOLD_MCP_ALLOWED_LANGUAGES}",
        },
        requestTimeoutMs: 330000,
        connectionTimeoutMs: 30000,
        supportsParallelToolCalls: false,
        toolFilter: {
          include: [
            "rookhold_run_code",
            "rookhold_job_result",
            "rookhold_job_events",
            "rookhold_cancel_job",
          ],
        },
      },
    },
  },

  // Required when the target agent is sandboxed. Merge this with an existing
  // sandbox tool policy rather than replacing a narrower allowlist.
  tools: {
    sandbox: {
      tools: {
        alsoAllow: ["bundle-mcp"],
      },
    },
  },
}
